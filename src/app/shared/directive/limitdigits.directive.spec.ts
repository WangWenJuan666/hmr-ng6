import { LimitdigitsDirective } from './limitdigits.directive';

describe('LimitdigitsDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitdigitsDirective();
    expect(directive).toBeTruthy();
  });
});
